import 'package:flutter/material.dart';

class CommentsDetailScreen extends StatelessWidget {
  const CommentsDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
